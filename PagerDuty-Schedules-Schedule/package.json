{
    "name": "pagerduty-schedules-schedule",
    "version": "0.1.0",
    "description": "AWS custom resource provider named PagerDuty::Schedules::Schedule.",
    "private": true,
    "main": "dist/handlers.js",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "npx tsc",
        "watch": "npx tsc -w",
        "prepack": "npm run build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@amazon-web-services-cloudformation/cloudformation-cli-typescript-lib": "^1.0.1",
        "axios": "^0.27.2",
        "class-transformer": "0.3.1",
        "pagerduty-common": "file:../PagerDuty-Common"
    },
    "devDependencies": {
        "@types/node": "^14.0.0",
        "typescript": "^4.1.2"
    },
    "optionalDependencies": {
        "aws-sdk": "^2.656.0"
    }
}
